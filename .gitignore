### Go ###
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.db
.DS_Store
# Test binary, built with `go test -c`
*.test

# Go compiled binaries (executable files, not directories)
/src/backend/backend

# All environment variables
secrets/
.env
.env.local

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
coverage.html

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work

# SonarQube scanner properties (contains secrets)
sonar-scanner.properties

# End

# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Jest coverage
coverage-js/

#backup
/src/backend/backups/
/backups/

VM-Gosearch_key.pem
gosearch

# Documentation files (personal notes, not for versioning)
IaC_Præsentation.md
TEST_COVERAGE_PLAN.md
